'use client';
export default function ViewerClient(){
  return <div className="mt-6 rounded-xl bg-white/80 backdrop-blur-md border border-white/30 p-6">
    3D viewer placeholder (client-only). Replace with R3F Canvas / AR when ready.
  </div>;
}
