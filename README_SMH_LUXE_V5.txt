SMH LUXE V5 — SAFE OVERLAY (no wipe)

Drop this zip over your existing project folder and choose "Replace" when prompted.

This overlay includes:
- components/preview/lux/*  (all Luxe sections)
- app/preview/home/lux  and  lux-ink
- components/layout/header.tsx  (luxury drawer: gradient + metallic shimmer, groups only)
- app/3d-showcase/ (SSR-safe)
- next.config.js (images.unoptimized, green build)

After extracting:
  pnpm install
  pnpm dev

Check:
  / (your site as-is)
  /preview/home/lux
  /preview/home/lux-ink
  /3d-showcase
